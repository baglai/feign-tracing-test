logging:
  level:
    reactivefeign:
      client:
        log: trace
    org:
      springframework:
        cloud:
          sleuth:
            instrument:
              reactor:
                log: trace